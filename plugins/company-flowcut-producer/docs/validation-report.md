# 0.3.0+codex.20260903095011 生成费用与套餐预检补充验证报告

验证日期：2026-09-03（Asia/Shanghai）

## 已通过

| 检查 | 结果 | 证据摘要 |
|---|---|---|
| 架构去耦 | 通过 | 运行时只包含只读 NAS 源、本机 SQLite、本机工作目录、ChatCut 和独立输出目录，不再探测外部任务服务 |
| Python 语法 | 通过 | 上下文、素材匹配器、任务库、验证器和测试模块可编译 |
| 单元测试 | 通过 | 共执行 50 项：49 项通过、1 项因当前 Windows 无符号链接权限跳过；新增覆盖正式未知费用审批、发布示例契约、旧非有限预估迁移、非有限数字拒绝、能力缺失/未知/过期、套餐不包含、审批顺序和只读提交前门禁 |
| 本机数据隔离 | 通过 | 预检和任务库均拒绝网络路径及源目录重叠；缓存、临时文件和日志固定到本机工作目录 |
| NAS 产品读取 | 通过 | 通过本机非入库配置精确读取验证产品，记录路径、修改时间和 SHA-256 |
| SQLite 初始化与迁移 | 通过 | Schema v3 新增 `capability_checks`；旧 `NaN`/非有限预估转成 `null + renew_exact_approval` 而不推断扣费同意；`read`/`list` 要求显式 `init`，不会在查询中隐式迁移 |
| SQLite 严格只读 | 通过 | 单元测试对查询前后数据库 SHA-256 与纳秒级修改时间进行比对，均保持不变 |
| SQLite 冒烟 | 通过 | 创建任务、状态/阶段迁移、指令、审批、ChatCut 引用、成本、旧项目接管和派生任务均通过 |
| 未知费用正式结构 | 通过 | `cost_estimate.status=unavailable` 时只接受 `amount=null`、来源和原因；批准还要求 `actual_charge`、本次操作范围与未知预估确认，0、`NaN` 和缺少确认均被拒绝 |
| 生成能力/套餐预检 | 通过 | `record-capability-check` 记录 provider/operation/feature/project/requested parameters、状态、来源和时间；`preflight-paid-operation` 对缺失、未知、错误、过期、`not_included`、旧审批和范围不一致保持阻塞 |
| 提交前只读性 | 通过 | `preflight-paid-operation` 使用 SQLite 只读连接，运行前后数据库 SHA-256 与纳秒级修改时间不变；只有新鲜 `available` 证据和随后完成的精确审批返回 ready |
| 产品配套媒体 | 通过 | 对本机验证产品只读发现配套图片候选并记录 SHA-256，不上传、不复制、不修改源文件；公开报告不保留真实产品名和目录 |
| 真实素材分类种子 | 通过 | 17 个候选生成 2 个主图、4 个使用场景、11 个功能证明/细节分类建议；全部保持待审核，ChatCut 合格数为 0，源路径不变 |
| 脚本到素材匹配 | 通过 | 硬约束覆盖产品范围、类别、标签、用途、媒体类型、方向、时长、复用、授权、品牌/声明、质量、SHA-256 和批准事实引用；排序与备选结果确定可复现 |
| 模板脚本服务 | 通过 | 只读发现管理员配置根，只选择 `approved` 模板；按产品、平台、画幅、语言、时长、标签和输出上限确定性解析，记录 revision、路径和 SHA-256 |
| 批量混剪计划 | 通过 | 同一请求可生成多命名版本、顺序变体和逐输出选择清单；同分素材按稳定 seed 轮换，重复运行保持相同 `batch_id`，共享导入路径去重 |
| ChatCut 交接闸门 | 通过 | 任一镜头 unresolved、事实引用未批准、素材待审核或哈希变化时，`ready_for_chatcut=false`，正式导入路径与 placements 为空 |
| 品牌/声明审核门 | 通过 | 脱敏的外部品牌、数字和比较性声明因未出现在批准产品文档中而进入 `publish_blocking`；内部审阅仍可继续 |
| 空结果审核 | 通过 | 人工确认无可见品牌或无内嵌声明时，可显式完成对应审核，不会因观察列表为空而永久阻塞 |
| ChatCut 精确脚本剪辑 | 通过 | 经用户授权在独立副本中删除一处配音 filler；限定目标语音轨提交，字幕同步，音频接缝处理完成，画面轨总时长保持 45 秒，删词点前后 9 个合成帧连续；原项目未修改，零积分且未导出 |
| 插件与技能结构 | 通过 | 插件验证器、技能验证器（UTF-8 模式）、Python 编译和 `git diff --check` 全部通过 |
| 公开仓库卫生 | 通过 | 真实 NAS 主机、内部路径、产品清单、SQLite、媒体和凭据均未进入仓库 |

本次补充验证没有继续修改 ChatCut 项目、发起生成或导出，也没有产生新的积分操作。此前暂停的项目和时间线保持原状。另一次经用户授权的写入回归只修改独立测试副本，没有覆盖原项目，也没有消耗积分。分类只产生本机元数据，不移动或重命名产品/素材源文件。

## 待部门配置

| 项目 | 当前状态 | 影响 |
|---|---|---|
| 共享素材根 | 未设置安全默认值 | 可读取产品并创建任务，但不能自动发现部门素材 |
| 批准模板根 | 未设置安全默认值 | 手工脚本路线可用；模板驱动批量混剪需配置 `COMPANY_VIDEO_TEMPLATE_ROOTS` 并放入已审核模板 |
| 成片输出根 | 未设置安全默认值 | 可以规划和编辑，不能报告已完成部门交付 |
| SQLite 多人模式 | 本机单任务单写者 | 初版适合小团队；共享队列需另建专属轻量存储服务 |

业务路径通过环境变量配置，不需要修改插件代码。SQLite、工作目录内容、真实素材和产品文档均未加入仓库。

## 真实试用门槛

1. 部门确认 `COMPANY_VIDEO_ASSET_ROOTS` 和 `COMPANY_VIDEO_OUTPUT_ROOT`。
2. 选定 3 个低风险任务，每个任务指定一位当前负责人。
3. 完成创建、编辑、人工闸门、质检、导出和会话恢复闭环。
4. 记录需求到首版时间、返工数、输出数量、积分/成本和恢复成功率。
